<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Edit Article</title>
    <style>
        /* Custom Styles */
        body {
            font-family: Arial, sans-serif;
            background: linear-gradient(135deg, #ffe53b, #ff2525); /* Gradient background */
            margin: 0;
            padding: 0;
            color: #333; /* Dark text color */
        }

        .container {
            max-width: 800px;
            margin: 20px auto;
            padding: 20px;
            background-color: #fff; /* White background */
            border-radius: 10px;
            box-shadow: 0px 4px 8px rgba(0, 0, 0, 0.1); /* Light shadow */
        }

        .header {
            background-color: #333; /* Dark background */
            color: #fff; /* White text color */
            padding: 20px;
            text-align: center;
            border-radius: 10px 10px 0 0; /* Rounded top corners */
        }

        .header h1 {
            margin: 0;
            font-size: 36px;
        }

        .form-group {
            margin-bottom: 20px;
        }

        label {
            font-weight: bold;
            color: #333; /* Dark text color */
        }

        textarea {
            height: 200px; /* Adjust as needed */
            width: 100%; /* Full width */
            padding: 10px;
            border: 1px solid #ccc;
            border-radius: 5px;
            font-size: 16px;
            resize: vertical; /* Allow vertical resizing */
        }

        .btn-cancel {
            background-color: #6c757d; /* Gray button */
            color: #fff; /* White text color */
            border: none;
            padding: 10px 20px;
            text-decoration: none;
            border-radius: 5px;
            transition: background-color 0.3s;
            cursor: pointer;
            font-size: 18px;
        }

        .btn-cancel:hover {
            background-color: #5a6268; /* Darker gray on hover */
        }

        .btn-submit {
            background-color: #42c5f5; /* Blue button */
            color: #fff; /* White text color */
            border: none;
            padding: 10px 20px;
            text-decoration: none;
            border-radius: 5px;
            transition: background-color 0.3s;
            cursor: pointer;
            font-size: 18px;
        }

        .btn-submit:hover {
            background-color: #2196f3; /* Darker blue on hover */
        }
    </style>
</head>
<body>
    <div class="container">
        <div class="header">
            <h1>Edit Article</h1>
        </div>
        <form action="/articles/<%=article.id%>?_method=PUT" method="POST">
            <div class="form-group">
                <label for="title">Title</label>
                <input required value="<%=article.title%>" type="text" name="title" class="form-control" id="title">
            </div>
            <div class="form-group">
                <label for="description">Description</label>
                <textarea name="description" id="description" class="form-control"><%=article.description%></textarea>
            </div>
            <div class="form-group">
                <label for="markdown">Markdown</label>
                <textarea required name="markdown" id="markdown" class="form-control"><%=article.markdown%></textarea>
            </div>
            <br>
            <button type="submit" class="btn btn-submit">Save</button>
            <a href="/" class="btn btn-cancel">Cancel</a>
        </form>
    </div>
</body>
</html>
